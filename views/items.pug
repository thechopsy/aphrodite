doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="shortcut icon", href="/assets/favicon.png")
    link(rel="stylesheet", href="/assets/items.css")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.4/jquery.min.js")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery-mobile/1.4.5/jquery.mobile.min.js")
    title= `${ seed || 'latest' } » aphrodite`
  body
    div.lanes
    script.
      const APP   =  '!{ app  }';
      const SEED  =  '!{ seed }';
      const SITES = ['!{ sites.join("', '") }'];
    script(src="/assets/items.js")
